# GPT Pro Plan Verification

- sessionId: 01KQSRCN626PGR4NYAV117TM2V
- model: pro
- timestamp: 

---

I’d run this as one commit per phase, with CI required green after every commit. I’m treating your verified post-merge state as authoritative; the public main branch aligns on the important mechanics: type: "module", tsx, co-located tsc emit, prepack/prepublishOnly, files, and current strict flags/scripts. 
GitHub
+3
GitHub
+3
GitHub
+3

For phases that involve strict compiler fixes, the exact source-file list should be generated by Phase 4 and committed in docs/migration/strict-error-inventory.md. Do not guess or opportunistically edit nearby files; touch tsconfig.json, the ledger, and only the files reported for that flag.

Recommendations on A–G

A. Strict rollout order

Use this order:

alwaysStrict

useUnknownInCatchVariables

noImplicitThis

strictBindCallApply

noImplicitAny

strictFunctionTypes

strictNullChecks

strictPropertyInitialization

strictBuiltinIteratorReturn

final "strict": true

then noUnusedLocals / noUnusedParameters

Reasoning: start with parse/runtime-mode and small local audits, then remove implicit any, then variance, then the large semantic nullability pass. strictPropertyInitialization should follow strictNullChecks, and strictBuiltinIteratorReturn needs to be handled before the final meta-flag because current TypeScript treats it as part of the strict family. TypeScript’s strict option enables a family of strict checks, and that family currently includes alwaysStrict, strictNullChecks, strictBindCallApply, strictFunctionTypes, strictPropertyInitialization, noImplicitAny, noImplicitThis, useUnknownInCatchVariables, and strictBuiltinIteratorReturn. 
TypeScript

B. Tests

Keep contract tests as .js. They read source text and assert regex contracts; converting them mostly creates annotations around test plumbing rather than surfacing app type errors. A public example is a contract test reading source files and asserting patterns in UI source and ui/dist. 
GitHub

Convert runtime-importing tests to .ts. Those tests import routes, lib, bin, config, or server and should participate in test typechecking; one visible runtime test imports route modules and config directly. 
GitHub

Keep node:test + tsx, add tsc -p tsconfig.tests.json. Node’s built-in test runner is stable, and tsx runs TypeScript directly but does not typecheck, so the typecheck must be a separate command. 
Node.js
+1

C. Large file splits

Split only lib/oauthProxy.ts now. It is 1003 lines, while the next listed candidates are under the 500-line rule. 
GitHub
 Keep lib/oauthProxy.ts as a public facade and move implementation into lib/oauthProxy/. Do not split routes/nodes.ts, bin/ima2.ts, or bin/commands/prompt.ts until they cross 500 or become strict-mode hotspots.

D. Artifact strategy

Keep co-located emit for this migration. Moving to dist/ would touch bin, package files, start, build configs, install smoke tests, possible deep imports, and contract tests all at once. Current build configs emit with outDir: ".", and package metadata maps ima2 to ./bin/ima2.js while shipping bin/, lib/, routes/, server.js, and config.js. 
GitHub
+2
GitHub
+2

Long term, dist/ is cleaner, but it should be a separate packaging migration after strict mode. NPM’s files field and bin behavior are package-boundary concerns, and changing them is a release-risk item rather than a type-safety item. 
npm Docs
+1

E. Scripts

Keep small scripts as .mjs. They are release/dev orchestration and should not require tsx during npm lifecycle execution. Add new guardrail scripts as .mjs too.

F. CI matrix

Add Node 24 LTS beside Node 22, keep Ubuntu and Windows. Node 22 and Node 24 are the supported LTS lines; Node 20 is already listed as End-of-Life with 2026-04-30 EOL, so do not add it to required CI. 
GitHub
 Current CI uses Ubuntu/Windows with Node 22 only and does not yet run the full strict/build/package gate. 
GitHub

G. Long-term TS source + JS runtime publish flow

Keep prepack as the build hook and prepublishOnly as the full validation hook. NPM runs prepack before tarball creation and prepublishOnly only on publish, which matches “build artifacts for the tarball, but block publish unless checks pass.” 
npm Docs
 Add npm pack --dry-run --json in CI to audit the package contents before publish. 
npm Docs

Standard verification gate

After Phase 1, every phase exits only when this passes, in this order:

npm run typecheck
npm run typecheck:tests
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


From Phase 3 onward, also run:

npm run check:lines


From Phase 16 onward, also run:

npm run pack:dry-run

Phase 1 — Add TS test runner support and runtime-test inventory

Why: Allow .test.ts files without converting tests yet, and produce a committed inventory that separates runtime-importing tests from source-text contract tests.

Scope

NEW:

tsconfig.tests.json — typecheck TS tests separately.

scripts/classify-tests.mjs — classifies tests as runtime-importing vs contract-only.

docs/migration/runtime-test-inventory.md — generated, committed inventory.

MODIFY:

scripts/run-tests.mjs — include .test.ts.

package.json — add typecheck:tests and test:inventory; wire into prepublishOnly.

Diff highlights

diff --git a/scripts/run-tests.mjs b/scripts/run-tests.mjs
@@
 const testDir = "tests";
 const files = readdirSync(testDir)
- .filter((f) => f.endsWith(".test.js"))
+ .filter((f) => /\.test\.[cm]?[jt]s$/.test(f))
  .map((f) => join(testDir, f))
  .sort();

diff --git a/package.json b/package.json
@@
- "test": "node scripts/run-tests.mjs",
+ "test": "node scripts/run-tests.mjs",
+ "typecheck:tests": "tsc --noEmit -p tsconfig.tests.json",
+ "test:inventory": "node scripts/classify-tests.mjs --check",
@@
- "prepublishOnly": "npm run typecheck && npm run ui:build && npm run build:server && npm run build:cli && npm run lint:pkg && npm run test:package-install",
+ "prepublishOnly": "npm run typecheck && npm run typecheck:tests && npm run test:inventory && npm run ui:build && npm run build:server && npm run build:cli && npm run lint:pkg && npm run test:package-install",

diff --git a/tsconfig.tests.json b/tsconfig.tests.json
new file mode 100644
@@
+{
+  "extends": "./tsconfig.json",
+  "compilerOptions": {
+    "noEmit": true,
+    "allowJs": true,
+    "checkJs": false,
+    "allowImportingTsExtensions": true,
+    "types": ["node"]
+  },
+  "include": [
+    "tests/**/*.test.js",
+    "tests/**/*.test.ts",
+    "tests/**/*.d.ts"
+  ],
+  "exclude": [
+    "node_modules",
+    "ui",
+    "integrations"
+  ]
+}

diff --git a/scripts/classify-tests.mjs b/scripts/classify-tests.mjs
new file mode 100755
@@
+#!/usr/bin/env node
+import { existsSync, readFileSync, readdirSync, writeFileSync } from "node:fs";
+import { join } from "node:path";
+
+const check = process.argv.includes("--check");
+const testDir = "tests";
+const outPath = "docs/migration/runtime-test-inventory.md";
+
+const runtimeImportPattern =
+  /(?:from\s+["']\.\.\/(?:lib|routes|bin)\/|from\s+["']\.\.\/(?:server|config)\.(?:ts|js)["']|import\(\s*["']\.\.\/(?:lib|routes|bin)\/|import\(\s*["']\.\.\/(?:server|config)\.(?:ts|js)["'])/;
+
+const files = readdirSync(testDir)
+  .filter((file) => /\.test\.[jt]s$/.test(file))
+  .sort();
+
+const runtime = [];
+const contract = [];
+
+for (const file of files) {
+  const path = join(testDir, file);
+  const text = readFileSync(path, "utf8");
+  (runtimeImportPattern.test(text) ? runtime : contract).push(path);
+}
+
+const renderList = (items) => items.map((item) => `- \`${item}\``).join("\n") || "- _none_";
+const output = [
+  "# Runtime test migration inventory",
+  "",
+  "Generated by `npm run test:inventory`.",
+  "",
+  "## Runtime-importing tests",
+  renderList(runtime),
+  "",
+  "## Contract-only tests",
+  renderList(contract),
+  "",
+].join("\n");
+
+if (check) {
+  if (!existsSync(outPath) || readFileSync(outPath, "utf8") !== output) {
+    console.error(`${outPath} is stale. Run: node scripts/classify-tests.mjs`);
+    process.exit(1);
+  }
+} else {
+  writeFileSync(outPath, output);
+}


Generate the inventory once:

node scripts/classify-tests.mjs


Verify

npm run typecheck
npm run typecheck:tests
npm run test:inventory
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: runner glob could unintentionally include a helper file. Mitigation: only files matching .test.js or .test.ts are included.

Rollback:

git revert <PHASE_1_SHA>


Exit criteria

All existing 539 tests still pass.

docs/migration/runtime-test-inventory.md is committed and stable.

No runtime test has been converted yet.

Phase 2 — Convert runtime-importing tests to TypeScript

Why: Runtime tests should surface TypeScript errors in mocks, route contexts, request bodies, and imported module usage. Contract tests stay JS.

Scope

NEW:

tests/*.test.ts for every file listed under ## Runtime-importing tests in docs/migration/runtime-test-inventory.md.

DELETE:

The corresponding tests/*.test.js files for those runtime tests.

MODIFY:

scripts/classify-tests.mjs — add --fail-js-runtime.

package.json — enforce that runtime-importing tests are no longer .js.

Runtime test files themselves — add TS annotations only where necessary.

Do not convert files listed under ## Contract-only tests.

Diff highlights

# Execute for every entry under "Runtime-importing tests":
git mv tests/<name>.test.js tests/<name>.test.ts

diff --git a/package.json b/package.json
@@
- "test:inventory": "node scripts/classify-tests.mjs --check",
+ "test:inventory": "node scripts/classify-tests.mjs --check --fail-js-runtime",

diff --git a/scripts/classify-tests.mjs b/scripts/classify-tests.mjs
@@
 const check = process.argv.includes("--check");
+const failJsRuntime = process.argv.includes("--fail-js-runtime");
@@
 if (check) {
@@
 }
+
+if (failJsRuntime) {
+  const jsRuntime = runtime.filter((path) => path.endsWith(".test.js"));
+  if (jsRuntime.length > 0) {
+    console.error("Runtime-importing tests must be .test.ts:");
+    for (const path of jsRuntime) console.error(`- ${path}`);
+    process.exit(1);
+  }
+}


Representative runtime test edits:

- function sseResponse(events) {
+ function sseResponse(events: unknown[]): Response {

- const calls = [];
+ const calls: Array<{ url: string; init: RequestInit; body: any }> = [];


Prefer unknown over any unless the test is intentionally inspecting arbitrary JSON:

- const body = await res.json();
+ const body = await res.json() as Record<string, unknown>;


For fetch mocks:

- globalThis.fetch = async (url, init) => {
+ globalThis.fetch = (async (url, init) => {
    ...
- };
+ }) as typeof fetch;


Verify

npm run typecheck
npm run typecheck:tests
npm run test:inventory
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: TS annotations in tests can hide mock mistakes if as any is overused. Policy: each converted file may use as unknown as typeof fetch for globalThis.fetch, but avoid broad file-level any.

Rollback:

git revert <PHASE_2_SHA>


Exit criteria

npm run test:inventory fails if a JS test imports runtime modules.

Contract-only tests remain .js.

Runtime-importing tests are .ts and included in typecheck:tests.

Phase 3 — Split lib/oauthProxy.ts behind a stable facade

Why: lib/oauthProxy.ts exceeds the 500-line rule and is the riskiest file to strictify. Split it before nullability and any cleanup, while keeping public imports stable.

Scope

NEW:

lib/oauthProxy/index.ts — barrel for internal split modules.

lib/oauthProxy/types.ts — shared OAuth proxy types.

lib/oauthProxy/prompts.ts — prompt constants and prompt builders.

lib/oauthProxy/references.ts — reference normalization and MIME handling.

lib/oauthProxy/errors.ts — OAuth error creation/classification/sanitization.

lib/oauthProxy/runtime.ts — readiness, URLs, timeout helpers, fetch wrapper.

lib/oauthProxy/streams.ts — SSE parsing and image stream readers.

lib/oauthProxy/generate.ts — generateViaOAuth, generateMultimodeViaOAuth.

lib/oauthProxy/edit.ts — editViaOAuth.

scripts/check-lines.mjs — 500-line source guard.

MODIFY:

lib/oauthProxy.ts — reduce to facade.

package.json — add check:lines.

Contract tests that explicitly read lib/oauthProxy.ts and assert internal implementation placement. Expected files to inspect and update:

tests/oauth-masked-edit-contract.test.js

tests/oauth-proxy-edit-mask-contract.test.js

tests/prompt-fidelity.test.js

tests/oauth-normalize.test.ts

tests/oauth-proxy-error-safety.test.ts

Do not modify route/bin consumers unless TypeScript import resolution requires it; the facade should preserve the public import path.

Diff highlights

diff --git a/lib/oauthProxy.ts b/lib/oauthProxy.ts
@@
-import { setJobPhase } from "./inflight.js";
-import { config } from "../config.js";
-import { logEvent } from "./logger.js";
-import { SAFETY_INTENT_POLICY } from "./promptSafetyPolicy.js";
-import { classifyUpstreamError, classifyUpstreamErrorCode } from "./errorClassify.js";
-import { compressReferenceB64ForOAuth } from "./referenceImageCompress.js";
-import { detectImageMimeFromB64, safeReferenceDiagnostics } from "./refs.js";
-...
-export async function generateViaOAuth(...) { ... }
-export async function generateMultimodeViaOAuth(...) { ... }
-export async function editViaOAuth(...) { ... }
+export * from "./oauthProxy/index.js";

diff --git a/lib/oauthProxy/index.ts b/lib/oauthProxy/index.ts
new file mode 100644
@@
+export {
+  PROMPT_FIDELITY_SUFFIX,
+  REAL_PERSON_RESEARCH_DIRECTIVE,
+  GENERATE_DEVELOPER_PROMPT,
+  GENERATE_NO_SEARCH_DEVELOPER_PROMPT,
+  EDIT_DEVELOPER_PROMPT,
+  EDIT_NO_SEARCH_DEVELOPER_PROMPT,
+  buildUserTextPrompt,
+  buildMultimodeSequencePrompt,
+  buildEditTextPrompt,
+} from "./prompts.js";
+
+export { waitForOAuthReady } from "./runtime.js";
+export { generateViaOAuth, generateMultimodeViaOAuth } from "./generate.js";
+export { editViaOAuth } from "./edit.js";

diff --git a/package.json b/package.json
@@
  "build:cli": "tsc -p tsconfig.bin.json && node scripts/fix-shebangs.mjs"
+,
+ "check:lines": "node scripts/check-lines.mjs"

diff --git a/scripts/check-lines.mjs b/scripts/check-lines.mjs
new file mode 100755
@@
+#!/usr/bin/env node
+import { readdirSync, readFileSync, statSync } from "node:fs";
+import { extname, join } from "node:path";
+
+const max = Number(process.env.MAX_TS_LINES || 500);
+const roots = ["server.ts", "config.ts", "lib", "routes", "bin"];
+const failures = [];
+
+function visit(path) {
+  const stat = statSync(path);
+  if (stat.isDirectory()) {
+    for (const entry of readdirSync(path)) visit(join(path, entry));
+    return;
+  }
+  if (extname(path) !== ".ts") return;
+  if (path.endsWith(".d.ts") || path.endsWith(".test.ts")) return;
+  const lines = readFileSync(path, "utf8").split(/\r?\n/).length;
+  if (lines > max) failures.push({ path, lines });
+}
+
+for (const root of roots) visit(root);
+
+if (failures.length > 0) {
+  for (const { path, lines } of failures) {
+    console.error(`${path}: ${lines} lines exceeds ${max}`);
+  }
+  process.exit(1);
+}


Contract test update pattern:

- const source = readSource("lib/oauthProxy.ts");
- assert.match(source, /function normalizeReferenceForOAuth/);
+ const source = readSource("lib/oauthProxy/references.ts");
+ assert.match(source, /function normalizeReferenceForOAuth/);
+
+ const facade = readSource("lib/oauthProxy.ts");
+ assert.match(facade, /export \* from "\.\/oauthProxy\/index\.js"/);


Verify

npm run typecheck
npm run typecheck:tests
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: subtle circular imports inside the new lib/oauthProxy/ directory. Mitigation: no behavior changes; facade preserves public exports; runtime tests from Phase 2 must cover OAuth/generate/edit paths.

Rollback:

git revert <PHASE_3_SHA>


Exit criteria

lib/oauthProxy.ts is a small facade.

Every new lib/oauthProxy/*.ts file is under 500 lines.

No consumer import path breaks.

npm run check:lines passes.

Phase 4 — Add strict rollout audit ledger

Why: Strict phases need exact file scopes. This phase creates a repeatable source of truth for which files each flag touches.

Scope

NEW:

scripts/strict-audit.mjs — runs tsc repeatedly with one strict flag at a time and records diagnostic file paths.

docs/migration/strict-error-inventory.md — generated ledger.

MODIFY:

package.json — add strict:audit.

Diff highlights

diff --git a/package.json b/package.json
@@
  "check:lines": "node scripts/check-lines.mjs",
+ "strict:audit": "node scripts/strict-audit.mjs",

diff --git a/scripts/strict-audit.mjs b/scripts/strict-audit.mjs
new file mode 100755
@@
+#!/usr/bin/env node
+import { existsSync, readFileSync, writeFileSync } from "node:fs";
+import { spawnSync } from "node:child_process";
+
+const check = process.argv.includes("--check");
+const outPath = "docs/migration/strict-error-inventory.md";
+
+const phases = [
+  ["alwaysStrict", ["--alwaysStrict", "true"]],
+  ["useUnknownInCatchVariables", ["--useUnknownInCatchVariables", "true"]],
+  ["noImplicitThis", ["--noImplicitThis", "true"]],
+  ["strictBindCallApply", ["--strictBindCallApply", "true"]],
+  ["noImplicitAny", ["--noImplicitAny", "true"]],
+  ["strictFunctionTypes", ["--strictFunctionTypes", "true"]],
+  ["strictNullChecks", ["--strictNullChecks", "true"]],
+  ["strictPropertyInitialization", ["--strictPropertyInitialization", "true"]],
+  ["strictBuiltinIteratorReturn", ["--strictBuiltinIteratorReturn", "true"]],
+  ["strict", ["--strict", "true"]],
+];
+
+function run(args) {
+  const result = spawnSync(
+    process.platform === "win32" ? "npx.cmd" : "npx",
+    ["tsc", "--noEmit", "-p", "tsconfig.json", "--pretty", "false", ...args],
+    { encoding: "utf8" },
+  );
+  return `${result.stdout || ""}${result.stderr || ""}`;
+}
+
+function filesFrom(output) {
+  const files = new Set();
+  for (const match of output.matchAll(/^(.+?\.ts)\(\d+,\d+\): error /gm)) {
+    files.add(match[1].replaceAll("\\", "/"));
+  }
+  return [...files].sort();
+}
+
+const sections = ["# Strict rollout error inventory", "", "Generated by `npm run strict:audit`.", ""];
+for (const [name, args] of phases) {
+  const files = filesFrom(run(args));
+  sections.push(`## ${name}`, "");
+  sections.push(files.length ? files.map((file) => `- [ ] \`${file}\``).join("\n") : "- [x] _no diagnostics_");
+  sections.push("");
+}
+
+const output = `${sections.join("\n")}\n`;
+if (check) {
+  if (!existsSync(outPath) || readFileSync(outPath, "utf8") !== output) {
+    console.error(`${outPath} is stale. Run: npm run strict:audit`);
+    process.exit(1);
+  }
+} else {
+  writeFileSync(outPath, output);
+}


Generate once:

npm run strict:audit


Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: audit output differs by TypeScript version if local lockfile is stale. Mitigation: run after npm ci and commit package-lock.json changes only when dependency changes are intentional.

Rollback:

git revert <PHASE_4_SHA>


Exit criteria

docs/migration/strict-error-inventory.md exists.

No strict flag is enabled yet.

Standard gate remains green.

Phase 5 — Enable alwaysStrict

Why: Lowest-risk strict-family flag. It makes TS parse in strict mode and emits strict-mode prologues where relevant. 
TypeScript

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Only files listed under ## alwaysStrict if diagnostics exist.

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
  "strict": false,
+ "alwaysStrict": true,
  "noImplicitAny": false,


Expected source changes: none, or rare fixes to non-strict syntax.

Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: generated JS diff changes by adding "use strict" in CommonJS-like outputs. This repo is ESM, so runtime risk is low.

Rollback:

git revert <PHASE_5_SHA>


Exit criteria

alwaysStrict: true.

No test or package artifact behavior changes.

Phase 6 — Enable useUnknownInCatchVariables

Why: Error handling becomes safer before broader any cleanup. This matters for OAuth/API error sanitization.

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Files listed under ## useUnknownInCatchVariables

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
  "alwaysStrict": true,
+ "useUnknownInCatchVariables": true,
  "noImplicitAny": false,


Representative fix:

- } catch (err) {
-   logEvent("oauth", "proxy_unavailable", { requestId, message: err?.message });
-   throw makeOAuthError("OAuth proxy is unavailable", { cause: err });
+ } catch (err: unknown) {
+   const message = err instanceof Error ? err.message : String(err);
+   logEvent("oauth", "proxy_unavailable", { requestId, message });
+   throw makeOAuthError("OAuth proxy is unavailable", { cause: err });
  }


Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: error serialization can change if String(err) differs from previous unsafe property access. Keep user-facing error messages covered by oauth-proxy-error-safety tests.

Rollback:

git revert <PHASE_6_SHA>


Exit criteria

All catch variables are narrowed before property access.

No new catch (err: any).

Phase 7 — Enable noImplicitThis

Why: Finds callbacks or nested functions that accidentally rely on an untyped this.

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Files listed under ## noImplicitThis

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
  "useUnknownInCatchVariables": true,
+ "noImplicitThis": true,
  "noImplicitAny": false,


Representative fix:

- return function () {
-   return this.value;
- };
+ return function (this: { value: string }) {
+   return this.value;
+ };


Prefer arrow functions when lexical this was intended:

- setTimeout(function () {
-   this.flush();
- }, 0);
+ setTimeout(() => {
+   this.flush();
+ }, 0);


Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: converting function to arrow changes this binding. Only use arrows where lexical binding is intended; otherwise add an explicit this: parameter.

Rollback:

git revert <PHASE_7_SHA>


Exit criteria

noImplicitThis: true.

No behavior-changing callback rewrites without tests.

Phase 8 — Enable strictBindCallApply

Why: Small blast radius; catches unsafe dynamic function invocation before broader callback typing.

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Files listed under ## strictBindCallApply

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
  "noImplicitThis": true,
+ "strictBindCallApply": true,
  "noImplicitAny": false,


Representative fix:

- return handler.apply(undefined, args);
+ return handler(...args);


For tuple-preserving helpers:

- function call(fn, args) {
-   return fn.apply(undefined, args);
+ function call<TArgs extends unknown[], TResult>(
+   fn: (...args: TArgs) => TResult,
+   args: TArgs,
+ ): TResult {
+   return fn(...args);
  }


Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: generic helper signatures can become too narrow. Keep helper APIs generic over tuples, not unknown[], when forwarding arguments.

Rollback:

git revert <PHASE_8_SHA>


Exit criteria

strictBindCallApply: true.

No added as any around call, bind, or apply.

Phase 9 — Enable noImplicitAny

Why: Biggest mechanical typing phase. Do it before nullability so null errors are attached to explicit types rather than inferred any.

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Files listed under ## noImplicitAny

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
- "noImplicitAny": false,
+ "noImplicitAny": true,


Representative fixes:

- function jsonResponse(body, status = 400) {
+ function jsonResponse(body: unknown, status = 400): Response {

- app.post("/api/generate", async (req, res) => {
+ app.post("/api/generate", async (req: Request, res: Response) => {

- const calls = [];
+ const calls: Array<{ url: string; init: RequestInit; body: Record<string, unknown> }> = [];


Rules for this phase:

// Prefer:
Record<string, unknown>
unknown[]
Map<string, unknown>

// Avoid:
any
Record<string, any>
Function


Allowed exception: narrow test mocks may use as unknown as typeof fetch.

Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: annotating too narrowly can break legitimate callers. For exported functions, preserve current runtime flexibility and validate internally.

Rollback:

git revert <PHASE_9_SHA>


Exit criteria

noImplicitAny: true.

No new explicit any unless documented inline with a one-line reason.

Runtime test TS annotations compile.

Phase 10 — Enable strictFunctionTypes

Why: After parameters are explicit, function variance errors become meaningful. This protects callbacks, route handlers, event handlers, and test mocks.

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Files listed under ## strictFunctionTypes

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
  "strictBindCallApply": true,
+ "strictFunctionTypes": true,
  "noImplicitAny": true,


Representative fix:

- const handler: (event: BaseEvent) => void = (event: SpecificEvent) => {
-   processSpecific(event);
- };
+ const handler: (event: BaseEvent) => void = (event) => {
+   if (!isSpecificEvent(event)) return;
+   processSpecific(event);
+ };


Express/request callbacks should use framework types at the boundary and narrow inside.

Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: quick casts can hide real callback mismatch. Prefer type guards over as.

Rollback:

git revert <PHASE_10_SHA>


Exit criteria

strictFunctionTypes: true.

No widened route/event handler that accepts less than callers can pass.

Phase 11 — Enable strictNullChecks

Why: Highest-value semantic strict flag. It catches missing config, absent DB rows, failed lookups, optional request fields, and nullable stream values.

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Files listed under ## strictNullChecks

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
- "strictNullChecks": false,
+ "strictNullChecks": true,
+ "strictPropertyInitialization": false,


Representative fixes:

- const row = db.prepare(sql).get(id);
- return row.path;
+ const row = db.prepare(sql).get(id) as { path: string } | undefined;
+ if (!row) return null;
+ return row.path;

- const port = server.address().port;
+ const address = server.address();
+ if (!address || typeof address === "string") {
+   throw new Error("Expected TCP server address");
+ }
+ const port = address.port;

- const value = config.imageModels.default;
+ const value = config.imageModels?.default ?? "gpt-5.4-mini";


Rules for this phase:

Prefer explicit T | null / T | undefined.

Use guard clauses.

Avoid ! except for values proven by immediately preceding checks.

Do not change HTTP response shapes without tests.

Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: this phase may expose latent behavior changes. Split the implementation internally by domain if needed, but keep it as one PR/commit only when the full gate is green.

Rollback:

git revert <PHASE_11_SHA>


Exit criteria

strictNullChecks: true.

No new non-null assertions without inline justification.

Package install smoke still passes.

Phase 12 — Enable strictPropertyInitialization

Why: With nullability on, class/lifecycle fields should be initialized or explicitly optional.

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Files listed under ## strictPropertyInitialization

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
- "strictPropertyInitialization": false,
+ "strictPropertyInitialization": true,


Representative fixes:

- private server: Server;
+ private server?: Server;

- class Store {
-   private db: Database.Database;
+ class Store {
+   private db: Database.Database;
+
+   constructor(db: Database.Database) {
+     this.db = db;
+   }
  }


Use definite assignment only for externally initialized lifecycle fields:

private app!: Express;


That should be rare and documented.

Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: converting fields to optional pushes checks into every callsite. Prefer constructor initialization when possible.

Rollback:

git revert <PHASE_12_SHA>


Exit criteria

strictPropertyInitialization: true.

Optional fields are actually optional at runtime.

Phase 13 — Enable strictBuiltinIteratorReturn

Why: Current TypeScript includes this in the strict family, so handling it before "strict": true avoids a surprise final flip.

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Files listed under ## strictBuiltinIteratorReturn

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
  "strictPropertyInitialization": true,
+ "strictBuiltinIteratorReturn": true,


Representative fix:

- const value = iterator.next().value;
+ const result = iterator.next();
+ if (result.done) return undefined;
+ const value = result.value;


Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: usually low unless custom iterators/generators are used.

Rollback:

git revert <PHASE_13_SHA>


Exit criteria

strictBuiltinIteratorReturn: true.

No unchecked .next().value on possibly-done iterators.

Phase 14 — Flip final "strict": true

Why: All current strict-family flags are already enabled. This makes the config honest and prevents future code from relying on non-strict behavior.

Scope

MODIFY:

tsconfig.json

docs/migration/strict-error-inventory.md

Expected source modifications: none. If source modifications are needed, the previous strict subflag phases missed something; fix the earlier phase instead.

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
- "strict": false,
+ "strict": true,
  "alwaysStrict": true,
  "useUnknownInCatchVariables": true,
  "noImplicitThis": true,
  "strictBindCallApply": true,
  "strictFunctionTypes": true,
  "noImplicitAny": true,
  "strictNullChecks": true,
  "strictPropertyInitialization": true,
  "strictBuiltinIteratorReturn": true,


Keep the explicit subflags even though they are redundant; they document the rollout and make future diffs clear.

Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: future TypeScript upgrades can add new strict checks under the meta-flag. Mitigation: TypeScript upgrades become their own PRs with this same gate.

Rollback:

git revert <PHASE_14_SHA>


Exit criteria

"strict": true.

Standard gate green.

No source changes in this phase unless caused by strict itself.

Phase 15 — Enable unused-code checks

Why: This is not a strict-family semantic flag, but after strict mode it keeps the tree clean and prevents dead migration scaffolding.

Scope

MODIFY:

tsconfig.json

Files reported by npm run typecheck

Diff highlights

diff --git a/tsconfig.json b/tsconfig.json
@@
- "noUnusedLocals": false,
- "noUnusedParameters": false,
+ "noUnusedLocals": true,
+ "noUnusedParameters": true,


Representative fixes:

- function handler(req: Request, res: Response, next: NextFunction) {
+ function handler(req: Request, res: Response, _next: NextFunction) {


TypeScript exempts parameters beginning with _ from noUnusedParameters, so use that for framework-required parameters. 
TypeScript

Delete unused locals rather than prefixing them:

- const debugPayload = buildDebugPayload(req);
  return res.json({ ok: true });


Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run check:lines
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install


Risk / Rollback

Risk: deleting apparently unused exports that are actually public API. Do not remove exported symbols in this phase unless tests and package contracts prove they are private.

Rollback:

git revert <PHASE_15_SHA>


Exit criteria

noUnusedLocals: true.

noUnusedParameters: true.

No public API removals.

Phase 16 — Make co-located publish artifacts deterministic

Why: Keep current co-located emit, but remove stale JS risk before prepack and publish. This is the safer alternative to a dist/ move during strict migration.

Scope

NEW:

scripts/clean-generated.mjs — deletes generated co-located JS artifacts only.

scripts/lint-package.mjs — replaces fragile inline node -e package lint.

MODIFY:

package.json

DELETE:

Inline node -e logic from lint:pkg.

Diff highlights

diff --git a/package.json b/package.json
@@
- "prepack": "npm run ui:build && npm run build:server && npm run build:cli",
+ "prepack": "npm run clean:artifacts && npm run ui:build && npm run build:server && npm run build:cli",
@@
- "lint:pkg": "node -e \"const p=require('./package.json'); const req=['name','version','bin']; for(const k of req){if(!p[k])throw new Error('missing '+k)} const mustInclude=['bin/','lib/','routes/','integrations/comfyui/ima2_gen_bridge/__init__.py','integrations/comfyui/ima2_gen_bridge/nodes.py','integrations/comfyui/ima2_gen_bridge/README.md','assets/card-news/templates/','server.js']; for(const f of mustInclude){if(!p.files.includes(f))throw new Error('files[] must include '+f)}\"",
+ "lint:pkg": "node scripts/lint-package.mjs && npm run check:lines",
+ "pack:dry-run": "npm pack --dry-run --json",
+ "clean:server-artifacts": "node scripts/clean-generated.mjs server",
+ "clean:cli-artifacts": "node scripts/clean-generated.mjs cli",
+ "clean:artifacts": "node scripts/clean-generated.mjs all",
@@
- "build:server": "tsc -p tsconfig.build.json",
- "build:cli": "tsc -p tsconfig.bin.json && node scripts/fix-shebangs.mjs"
+ "build:server": "npm run clean:server-artifacts && tsc -p tsconfig.build.json",
+ "build:cli": "npm run clean:cli-artifacts && tsc -p tsconfig.bin.json && node scripts/fix-shebangs.mjs"

diff --git a/scripts/clean-generated.mjs b/scripts/clean-generated.mjs
new file mode 100755
@@
+#!/usr/bin/env node
+import { existsSync, readdirSync, rmSync, statSync } from "node:fs";
+import { extname, join } from "node:path";
+
+const mode = process.argv[2] || "all";
+const modes = new Map([
+  ["server", ["server.js", "config.js", "lib", "routes"]],
+  ["cli", ["bin"]],
+  ["all", ["server.js", "config.js", "lib", "routes", "bin"]],
+]);
+
+const roots = modes.get(mode);
+if (!roots) {
+  console.error(`Unknown clean mode: ${mode}`);
+  process.exit(1);
+}
+
+function clean(path) {
+  if (!existsSync(path)) return;
+  const stat = statSync(path);
+  if (stat.isDirectory()) {
+    for (const entry of readdirSync(path)) clean(join(path, entry));
+    return;
+  }
+  if (extname(path) === ".js" || path.endsWith(".js.map")) {
+    rmSync(path, { force: true });
+  }
+}
+
+for (const root of roots) clean(root);

diff --git a/scripts/lint-package.mjs b/scripts/lint-package.mjs
new file mode 100755
@@
+#!/usr/bin/env node
+import { existsSync, readFileSync } from "node:fs";
+
+const pkg = JSON.parse(readFileSync("package.json", "utf8"));
+
+for (const key of ["name", "version", "type", "bin", "files"]) {
+  if (!pkg[key]) throw new Error(`package.json missing ${key}`);
+}
+
+if (pkg.type !== "module") throw new Error('package.json type must remain "module"');
+if (pkg.bin?.ima2 !== "./bin/ima2.js") throw new Error('bin.ima2 must be "./bin/ima2.js"');
+
+const mustInclude = [
+  "bin/",
+  "lib/",
+  "routes/",
+  "integrations/comfyui/ima2_gen_bridge/__init__.py",
+  "integrations/comfyui/ima2_gen_bridge/nodes.py",
+  "integrations/comfyui/ima2_gen_bridge/README.md",
+  "assets/card-news/templates/",
+  "ui/dist/",
+  "server.js",
+  "config.js",
+];
+
+for (const entry of mustInclude) {
+  if (!pkg.files.includes(entry)) throw new Error(`files[] must include ${entry}`);
+}
+
+const generatedMustExist = [
+  "server.js",
+  "config.js",
+  "bin/ima2.js",
+  "lib/oauthProxy.js",
+  "routes/generate.js",
+];
+
+for (const file of generatedMustExist) {
+  if (!existsSync(file)) throw new Error(`generated artifact missing: ${file}`);
+}


Verify

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install
npm run pack:dry-run


Risk / Rollback

Risk: clean-generated.mjs could delete a hand-authored .js file under lib, routes, or bin. Your verified state says source code is 100% TS and those JS files are generated, so this is acceptable. Do not include scripts/ in clean targets.

Rollback:

git revert <PHASE_16_SHA>


Exit criteria

Clean build produces all expected JS artifacts.

lint:pkg checks real generated artifacts after build.

npm run pack:dry-run succeeds.

Phase 17 — Expand CI matrix and align Node support

Why: CI should enforce the full migration gate on supported LTS lines and both OSes. Current CI only runs Node 22 on Ubuntu/Windows with UI build, tests, and package lint. 
GitHub

Scope

MODIFY:

.github/workflows/ci.yml

package.json

README.md only if it names Node >=20

Diff highlights

diff --git a/package.json b/package.json
@@
  "engines": {
-   "node": ">=20"
+   "node": ">=22"
  },

diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
@@
     strategy:
       fail-fast: false
       matrix:
-        os: [ubuntu-latest, windows-latest]
+        os: [ubuntu-latest, windows-latest]
+        node-version: [22, 24]
@@
     - uses: actions/setup-node@v4
       with:
-        node-version: '22'
+        node-version: ${{ matrix.node-version }}
+        cache: npm
@@
     - name: Install root deps
       run: npm ci
 
     - name: Install ui deps
       run: npm --prefix ui install --no-audit --no-fund
 
+    - name: Typecheck app
+      run: npm run typecheck
+
+    - name: Typecheck tests
+      run: npm run typecheck:tests
+
+    - name: Build server
+      run: npm run build:server
+
+    - name: Build cli
+      run: npm run build:cli
+
     - name: Build ui
-      run: npm --prefix ui run build
+      run: npm run ui:build
 
     - name: Run tests
       run: npm test
-      timeout-minutes: 3
+      timeout-minutes: 5
+
     - name: Lint package.json
-      if: matrix.os == 'ubuntu-latest'
+      if: matrix.os == 'ubuntu-latest' && matrix.node-version == 24
       run: npm run lint:pkg
+
+    - name: Package install smoke
+      run: npm run test:package-install
+
+    - name: Pack dry run
+      if: matrix.os == 'ubuntu-latest' && matrix.node-version == 24
+      run: npm run pack:dry-run


README patch, only where applicable:

- Requires Node.js >=20.
+ Requires Node.js >=22.


Verify

Local:

npm run typecheck
npm run typecheck:tests
npm run strict:audit -- --check
npm run ui:build
npm run build:server
npm run build:cli
npm test
npm run lint:pkg
npm run test:package-install
npm run pack:dry-run


Remote required checks:

ubuntu-latest / Node 22
ubuntu-latest / Node 24
windows-latest / Node 22
windows-latest / Node 24


Risk / Rollback

Risk: Node 24 exposes native dependency install differences for sharp or better-sqlite3. That is precisely why Node 24 should be added before release. Risk: changing engines.node to >=22 creates warnings for Node 20 users, but Node 20 is already EOL.

Rollback:

git revert <PHASE_17_SHA>


Exit criteria

All four CI cells pass.

Package smoke passes on Windows and Ubuntu.

engines.node matches tested supported lines.

Deferred dist/ migration shape

Do not include this in the strict/test/scripts sequence. After Phase 17, a separate packaging PR can move to dist/:

diff --git a/tsconfig.build.json b/tsconfig.build.json
@@
- "outDir": ".",
+ "outDir": "dist",

diff --git a/tsconfig.bin.json b/tsconfig.bin.json
@@
- "outDir": ".",
+ "outDir": "dist",

diff --git a/package.json b/package.json
@@
- "start": "node bin/ima2.js serve",
+ "start": "node dist/bin/ima2.js serve",
@@
- "ima2": "./bin/ima2.js"
+ "ima2": "./dist/bin/ima2.js"
@@
- "bin/",
- "lib/",
- "routes/",
- "server.js",
- "config.js"
+ "dist/",


That PR needs its own deep-import compatibility decision. Node treats .js as ESM when nearest package.json has "type": "module", so the emitted dist/**/*.js remains ESM-compatible. 
Node.js
 Adding or tightening exports can encapsulate public entry points and break unlisted deep imports, so do it deliberately rather than as part of strict mode. 
Node.js

Dependency graph
Phase 1  TS test runner + inventory
  └─> Phase 2  Runtime tests to TS
        └─> Phase 3  oauthProxy split + line guard
              └─> Phase 4  strict audit ledger
                    └─> Phase 5  alwaysStrict
                          └─> Phase 6  useUnknownInCatchVariables
                                └─> Phase 7  noImplicitThis
                                      └─> Phase 8  strictBindCallApply
                                            └─> Phase 9  noImplicitAny
                                                  └─> Phase 10 strictFunctionTypes
                                                        └─> Phase 11 strictNullChecks
                                                              └─> Phase 12 strictPropertyInitialization
                                                                    └─> Phase 13 strictBuiltinIteratorReturn
                                                                          └─> Phase 14 strict true
                                                                                └─> Phase 15 noUnusedLocals/noUnusedParameters
                                                                                      └─> Phase 16 deterministic co-located artifacts
                                                                                            └─> Phase 17 CI matrix + Node engine policy


The only optional branch is the later dist/ migration, which depends on Phase 17 but should not block the strict-mode migration.
